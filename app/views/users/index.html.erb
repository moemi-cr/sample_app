<% provide(:title, 'All users')%>
<h1>All Users</h1>

<%= will_paginate %>
<ul class="users">
    <% @users.each do |user| %>
        <%= render @users %>
        <!--- _user.html.erbを探しに行く --->
    <% end %>        
</ul>

<%= will_paginate %>
